# devops-with-docker-express-app
A simple Node.js/Express app with a CI/CD pipeline using GitHub Actions and Docker.  Automatically builds and pushes a Docker image to Docker Hub on every push to main.
